{
  "name": "chatai-demo",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "debug": "node --inspect ./src/worker.js",
    "deploy": "wrangler deploy",
    "start": "wrangler dev"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20230419.0",
    "typescript": "^5.0.4",
    "wrangler": "^3.0.0"
  },
  "dependencies": {
    "@pinecone-database/pinecone": "^0.1.6",
    "@vespaiach/axios-fetch-adapter": "^0.3.1",
    "openai": "^3.3.0"
  }
}
